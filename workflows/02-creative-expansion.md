# Creative Expansion Workflow

Use `creative-expansion` after `invention/invention-map.md` exists. Generate implementation variants, claim broadening ideas, adjacent use cases, productization scenarios, and risk-reducing fallback claims. Every item must include `Required Evidence` and `Score Use: Not allowed until verified`.
